const express = require("express");
const router = express.Router();

// Controllers

// Middlewares

// Routes
// /auth/login
router.post("/login");

// /auth/signup
router.post("/signup");

module.exports = router;
